package com.ziplly.app.client.activities.util;

import com.ziplly.app.model.CouponDTO;

public class CouponUtil {
	
	public static boolean isActive(CouponDTO coupon) {
//		Date now = new Date();
//		if (coupon.getQuantityPurchased() == coupon.getQuanity()) {
//			return false;
//		}
//
//		if (coupon.getStartDate().before(now) && coupon.getEndDate().after(now)) {
//			return true;
//		}
		return true;
	}
}
